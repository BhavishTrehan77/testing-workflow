services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      MONGO_URI: mongodb://mongodb:27017/connectionmadesuccess
    depends_on:
      - mongodb
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    
